# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
DB_HOST=db_hostname
DB_USER=db_username
DB_PASSWORD=db_password
DB_NAME=db_name

# JWT Configuration
JWT_SECRET=jwtsecret
JWT_EXPIRES_IN=1h

# Email Configuration (Gmail for sending OTP)
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_16_char_app_password

# Instructions to generate Gmail App Password:
# 1. Go to Google Account Security (https://myaccount.google.com/security)
# 2. Enable 2-Step Verification
# 3. Go to "App Passwords"
# 4. Select "Mail" as app, give a name for the password (e.g., "Notes App Nodemailer")
# 5. Copy the 16-character password and set it as EMAIL_PASS
